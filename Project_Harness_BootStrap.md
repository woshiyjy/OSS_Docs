# 项目 Harness 启动种子

- 当前版本：0.3
- 用途：新项目初始化、现有 Harness 体检与渐进升级
- 自动执行：否，仅在用户明确要求时使用
- 运行时入口：初始化完成后以目标项目根的 `AGENTS.md` 为准

本文件是一次性启动种子和可重复使用的体检清单，不是日常运行规则。它本身保持不变；项目应用过哪个版本、何时应用、应用到哪一层，只记录在项目自己的知识索引或交接记录中。

## 使用方式

只读评审：

> 请完整阅读 `PROJECT_HARNESS_BOOTSTRAP.md`，按评审模式检查当前项目。不要修改任何文件；请报告真正的项目根、现有 Harness 职责映射、缺口、冲突、风险和拟调整清单。

初始化或升级：

> 请完整阅读 `PROJECT_HARNESS_BOOTSTRAP.md`，按执行模式初始化或整理当前项目 Harness。先判定真正的项目根，尊重现有结构、语言和命名；已有 Harness 时只补缺口。涉及移动、删除、改名、改变公开范围或整体重写时先征得同意。

---

## 1. 任务目标

建立一套轻量、可维护、可跨会话和跨 Agent 使用的项目约束与外部记忆系统，使新的参与者能够快速回答：

1. 这个项目要实现什么？
2. 当前实际做到哪里？
3. 哪些边界和原则不能违反？
4. 正在进行什么工作，有什么风险？
5. 下一步应该做什么，怎样证明完成？

只建立解决这些问题所必需的内容，不追求目录齐全，不制造空文件。

## 2. 授权模式

### 评审模式

当用户要求评审、分析、诊断、建议或明确要求只读时：

- 只读取和分析，不创建、修改、移动或删除文件。
- 不更新项目状态、计划、交接、决策或种子应用记录。
- 在回复中列出拟复用、拟新增、拟局部修改和保持不变的内容。
- 等获得执行授权后再落盘。

### 执行模式

当用户明确要求初始化、建立、整理或升级时：

- 无现有 Harness：按最小职责直接初始化。
- 已有 Harness：先做职责映射，默认只补缺口，不重建第二套系统。
- 现有文件只做必要的局部修改，保留人工内容和有效历史。
- 移动、删除、改名、合并目录、改变公开范围或整体重写前先确认。

任何“记忆需要更新”的规则都不能突破当前任务的只读或写入授权。

## 3. 判定真正的项目根

当前打开的工作区不一定是项目根。修改前先找到承载主要交付物、代码或正文、配置、脚本、数据和项目文档的最小共同目录。

按以下情况处理：

- **单一项目**：Harness 建在实际项目根。
- **外层只是容器**：内层项目保存完整 Harness；外层最多保留短导航，不复制项目记忆。
- **多个独立项目**：各自维护 Harness；只有真实存在跨项目协调时，外层才维护总体导航和状态。
- **单体仓库或多模块项目**：根 `AGENTS.md` 保存全局规则；只有子目录确实存在不同边界时才增加局部 `AGENTS.md`。
- **无法确定**：列出候选目录和依据，向用户确认后再写入。

评审或执行报告必须写明：

```text
工作区根：
目标项目根：
判定依据：
是否需要外层导航：
```

不得未经允许把已有记忆系统迁移到另一层。

## 4. 核心原则

- **先检查，后创建**：先理解现状，再决定结构。
- **职责优先于文件名**：示例路径不是强制标准；已有中文或其他语言结构时保持原语言。
- **单一事实来源**：同一职责只由一个权威文件承担，其他地方链接引用。
- **最小化**：没有实际用途的文件、目录和流程不创建。
- **不编造**：无法核实的信息标记“待确认”。
- **值得复用才写入**：只记录跨会话仍有价值的事实、决定、进度、风险和下一步；不记录聊天流水账和临时探索过程。
- **证据可核验**：完成和状态声明应指向实际文件、数据、测试、构建结果或人工核验依据。
- **保护现有工作**：不覆盖、撤销或改写用户和其他 Agent 的未完成工作。
- **稳定引用**：被链接的计划和决策尽量保持路径稳定，用状态字段表达生命周期。
- **公开与内部隔离**：内部状态、计划和决策默认不进入公开网站或对外交付物。

## 5. 分清三类事实

不要用一套证据顺序同时判断“应该是什么”和“现在是什么”。

### 目标与约束

优先依据：

1. 当前用户在其授权范围内、且不违反更高层安全、合规和组织政策的明确要求。
2. 有权的业务或产品负责人最新确认。
3. 合同、批准后的正式需求、政策和已确认决策。
4. 旧计划、交接记录和 Agent 推断。

### 已发生的业务事实

优先依据原始凭证、现场记录、系统数据和经核验资料，并保留来源、时间范围和口径。

### 当前实现状态

优先依据正在运行的系统、当前代码与配置、数据库结构、实际产物、测试、构建和日志结果。

当三者不一致时，明确记录：

```text
预期目标：
业务实际：
当前实现：
差距与待确认事项：
```

不得因为代码已经这样实现，就让代码覆盖已确认目标；也不得因为需求已经写下，就声称功能已经实现。

动态状态的核验记录应按需要注明时间、环境、版本或入口、使用身份以及证据范围。

## 6. 初始化前检查

先进行只读盘点：

- 项目目标、主要使用者和交付物。
- 项目属于软件、文档网站、办公资料、数据处理还是混合类型。
- 真正的项目根和外层目录作用。
- 已有 `README.md`、`AGENTS.md`、兼容入口及局部指令。
- 已有知识索引、状态或交接、决策、架构、业务规则、运维和计划资料。
- Git 工作区和近期历史；只查看，不撤销现有改动。
- 实际存在的构建、预览、测试、检查、导出或人工验收方式。
- 公开发布目录、内部资料目录和敏感信息边界。
- 当前活动工作、阻塞、风险和待确认事项。

先输出或建立职责映射：

| 必要职责 | 现有承担文件 | 状态 | 处理方式 |
|---|---|---|---|
| Agent 运行入口 |  | 已有 / 缺失 | 复用 / 新建 / 局部补充 |
| 项目知识索引 |  |  |  |
| 当前状态或交接 |  |  |  |
| 关键决策 |  |  |  |
| 复杂任务计划 |  |  | 仅在需要时建立 |

只有缺失信息会实质改变结果时才向用户提问。

## 7. 最小运行时系统

项目需要承担以下职责，但不强制采用固定目录：

```text
README.md                         面向人的项目入口；已有时复用
AGENTS.md                         Agent 运行协议和知识地图
<知识索引>                        长期资料导航及何时读取
<当前状态或当前交接>              当前阶段、活动任务、阻塞、风险和下一步
<关键决策>                        影响未来工作的长期决定
<复杂任务计划>                    仅在真正复杂、跨会话任务出现时建立
```

如果项目没有现有约定，可以按项目主要语言选择清晰名称，例如：

```text
中文：项目记忆/知识索引.md、当前状态.md、关键决策.md、任务计划/
英文：project-memory/INDEX.md、STATUS.md、DECISIONS.md、plans/
```

如果已有 `项目文档/`、`项目交接/`、`docs/` 或其他成熟结构，直接复用，不创建同义目录。

如果 `docs/` 是 VitePress 或其他网站的发布源目录，内部状态和计划放在发布目录之外；如果 `docs/` 只是内部工程文档目录，则可以继续使用。

不要预先创建空的任务计划目录。第一次出现确实需要跨会话接力的复杂任务时再创建计划。

## 8. `AGENTS.md` 的要求

`AGENTS.md` 是短地图和行为协议，不是百科全书，建议不超过 150 行。至少说明：

- 项目目标、项目根和适用范围。
- 权威资料路径、用途以及何时读取。
- 目标、业务事实和当前实现的判断规则。
- 必须遵守的边界、安全规则和公开范围。
- 会话开始、复杂任务计划、交接和独立审查流程。
- 实际存在的验证命令或人工检查方式。
- 当前项目不同任务类型的完成标准。

不要放入频繁变化的任务进度、大段历史、聊天记录、低频完整教程、通用常识，或已经由工具自动执行的大量格式规则。

需要兼容只读取特定文件名的 Agent 时，让工具专用入口指向 `AGENTS.md`，不要复制一套独立规则。

## 9. 状态、计划与交接

### 当前状态或当前交接

只保存项目级仪表盘：目标、当前阶段、活动任务链接、主要阻塞、风险和下一步。不要复制任务计划里的详细进度。

最近核验信息按需记录：日期、环境、版本或入口、核验身份和证据范围。

### 复杂任务计划

简单讨论、只读查询、一次性小修改通常不建计划。符合以下任一情况时考虑创建：

- 预计跨多个会话或多个 Agent。
- 跨多个阶段、模块、系统或交付物。
- 存在依赖、风险、验收或回滚要求。
- 中断后仅靠聊天记录难以安全恢复。

计划使用稳定任务编号和稳定路径，至少记录：

```text
状态：拟定 / 进行中 / 阻塞 / 已完成 / 已取消
目标与范围：
完成标准：
起始事实：
进度：
关键取舍：
验证结果：
交接：已完成、当前停点、剩余工作、风险、下一步
```

长期决定写入权威决策文档，任务计划只保留必要引用。完成任务时优先修改状态，不移动文件；如果现有体系采用归档移动，应同步更新全部引用。

## 10. 独立审查协议

审查 Agent 应：

- 从需求、正式决策、实际文件或代码、差异和验证结果独立判断。
- 不把前一个 Agent 的完成声明当作证据。
- 分别检查“是否做对目标”和“当前实现是否可靠”。
- 每个问题给出具体证据、影响和建议方向。
- 默认只审查、不修改，除非用户明确要求修复。
- 未发现问题时说明检查范围、证据和未验证部分。

## 11. 敏感信息

- 不读取与任务无关的凭据内容；确认文件存在不等于需要打开。
- 不把密码、API Key、Secret、JWT、Cookie、验证码、访问令牌、私钥、`.env` 内容或私人授权配置写入项目记忆和回复。
- 示例使用明显的占位符或脱敏值。
- 发现疑似泄漏时不复述秘密，只报告位置、风险类型和处理建议。
- 初始化完成前检查新增和修改文档是否含敏感信息。

内部仓库同样适用以上规则。

## 12. 按项目类型适配

- **软件项目**：按需记录架构边界、规格与验收、构建测试、Lint、部署恢复、安全和技术债务。
- **VitePress 或文档网站**：按需记录内容规范、站点结构、术语、链接检查、预览和发布；内部记忆不得意外公开。
- **数据项目**：记录数据来源、时间范围、处理口径、完整性或抽样检查和可追溯证据。
- **办公资料项目**：记录交付物、版本、来源、处理与审核流程、审批节点和人工检查清单。
- **混合项目**：明确软件、公开内容、数据和内部资料边界，但保持一个统一运行入口和当前状态入口。

不要假设所有项目都需要 Lint 或 CI。已有自动检查时记录怎样运行；关键规则只能靠文字提醒时可标记“尚未机械验证”，但初始化阶段不擅自搭建复杂自动化。

## 13. 完成与验证

执行完成前检查：

- 已明确工作区根、目标项目根和 Harness 适用范围。
- 操作符合评审或执行授权。
- 没有建立第二套事实源，也没有破坏现有语言和命名。
- `AGENTS.md` 简短，且每个引用都说明何时读取。
- README、`AGENTS.md`、知识索引、状态和计划中的本地链接及引用路径实际存在。
- 目标、业务事实和当前实现没有混为一谈。
- 状态来自实际核验，并带有必要的时间和环境信息。
- 没有无意义的空目录、空文档和聊天流水账。
- 内部资料没有进入公开范围，新增文档不含敏感信息。
- 项目应用的种子版本、日期和目标根只记录在项目知识索引或交接记录中。

按任务类型核验：

- 文档：内容目标、来源、术语、结构、链接、构建或预览和公开边界。
- 软件：验收标准、相关测试、Lint、构建或人工检查，以及未验证风险。
- 数据：来源、时间范围、口径、完整性、抽样结果和敏感信息处理。
- 办公：交付格式、版本、完整性、审批要求和待人工确认事项。
- 审查：范围、证据、发现、未验证部分以及是否修改文件。

### 冷启动测试

初始化或升级后，用一个不了解历史的新会话进行测试。它只先阅读 `AGENTS.md`、当前状态和一个相关专题文件，然后回答：

1. 项目目标是什么？
2. 当前做到哪里？
3. 哪些事项不能擅自改变？
4. 当前主要风险或阻塞是什么？
5. 下一步是什么，如何验证完成？

如果回答错误或需要大量额外搜索，优先修正知识地图和当前状态，而不是继续增加更多常驻规则。

如果当前环境无法真正启动新会话，提供上述测试提示并标记“冷启动待验证”，不得假称已经通过。

## 14. 完成后的报告

评审模式报告：

- 工作区根、目标项目根及判定依据。
- 现有 Harness 的职责映射。
- 已具备能力、缺口、冲突和风险。
- 拟复用、新增、局部修改和保持不变的内容。
- 需要用户决定的事项。

执行模式报告：

- 项目根、项目类型和适用范围。
- 创建、更新、复用和保持不变的文件。
- 当前活动工作、阻塞和下一步。
- 未确认信息和未验证部分。
- 文字规则与自动或人工检查的对应情况。
- 链接检查、敏感信息检查和冷启动测试结果。

不得只回复“完成”；报告必须可以核对。

## 版本变化

- **0.3**：收敛为简洁种子；应用记录改为项目内单一来源；补充链接检查、冷启动测试、授权限定、三类事实和状态/计划职责边界。
- **0.2**：增加评审与执行模式、项目根判断、事实分轨、稳定计划路径和敏感信息规则。
- **0.1**：建立最小项目记忆、复杂任务计划、交接与独立审查的初始方案。
