# AGENTS.md — 口感番茄知识库 Agent 运行协议

## 项目定位

- **目标**：维护「口感番茄知识库」——面向口感番茄（高附加值樱桃番茄）种植的公开知识网站，同时作为 AI 智能体外挂知识库。
- **项目根**：本目录（`/Users/yuanjingyuan/Documents/Projects/OSS_Doc`）。单一项目，无外层容器。
- **类型**：VitePress 纯静态文档网站（阿里云 OSS+CDN → docs.wehifun.cn），通过 `llms.txt` 向 AI 智能体开放检索，无自建服务依赖。
- **仓库**：`github.com:woshiyjy/OSS_Docs.git`，main 分支，公开仓库。

## 权威资料地图（何时读什么）

| 文件 | 职责 | 何时读取 |
|---|---|---|
| `README.md` | 面向人的项目入口、架构与部署说明 | 首次接触项目、部署/集成问题 |
| `AGENTS.md` | 本文件：运行协议与知识地图 | 每次会话开始 |
| `项目记忆/知识索引.md` | 内容底本与工件导航 | 需要找原文、底本、提取工件时 |
| `项目记忆/当前状态.md` | 当前阶段、活动任务、风险、下一步 | 每次会话开始；动工前 |
| `项目记忆/关键决策.md` | 长期有效决定（内容与架构边界） | 做内容/结构决策前 |
| `docs/log.md` | 站点内容变更日志（**公开**，随站发布） | 每次内容变更后追加 |
| `TEMP/` | 原始文档提取工件（gitignore，不入库） | 需要对照原文时 |
| `Project_Harness_BootStrap.md` | Harness 种子 v0.3（一次性启动/体检清单） | 仅用户明确要求体检/升级时 |

**规则**：项目记忆只放根目录 `项目记忆/`，**绝不放入 `docs/`**（`docs/` 是 VitePress 发布源，入 docs 即公开）。

## 三类事实的判断规则

- **目标与约束**：以用户明确要求 > `项目记忆/关键决策.md` > 旧交接记录为准。
- **业务事实**（手册原文、品种数据、价格）：以原始凭证为准（TEMP/ 提取稿、来源标注），保留来源与口径。
- **实现状态**（站点现在有什么）：以 `docs/` 实际文件、`npm run build` 结果、git 历史为准。

三者不一致时，在 `项目记忆/当前状态.md` 记录「预期目标 / 业务实际 / 当前实现 / 差距与待确认」，不得互相覆盖。

## 必须遵守的边界

1. **发布隔离**：`docs/` 内不放内部状态、计划、密钥；根目录记忆文件不含敏感信息（仓库公开）。
2. **溯源内容全站不收录**：GLOBALG.A.P+区块链溯源、供应链穿透相关内容一律不落库（用户另行单独撰写）。
3. **采后处理板块冻结**：内容以《宁夏樱桃番茄采后处理技术规程》为准，不再改动。
4. **正文大段原文搬运**：手册内容只做归类+frontmatter 适配，不缩写不混写；侧边栏最多两层，分组顺序与原手册章节顺序一致。
5. **品种卡唯一落点**：品种信息只落 `docs/品种和育苗/` 品种卡，产区手册不设品种章；多来源合并须标注来源；真冲突并列标来源、不裁决。
6. **工作流**：本地 `npm run build` 零死链 + 用户预览确认后，才 commit + push（push main 即部署）。
7. **不入库**：`TEMP/`、`*.pem`、`CLAUDE.md`、`.DS_Store` 已 gitignore；不读取与任务无关的凭据内容。
8. **中宁为非核心产区**：中宁手册内容从简，不与海原手册平权扩写。

## 会话流程

- **开始**：读本文件 → `项目记忆/当前状态.md` → 按任务读 `项目记忆/知识索引.md` 与相关专题。
- **复杂任务**（跨会话/多阶段/有验收要求）：在 `项目记忆/任务计划/` 建计划文件（稳定路径+状态字段）；平时不预建。
- **收尾**：更新 `项目记忆/当前状态.md`；内容变更追加 `docs/log.md`；运行验证命令后再报告。
- **交接**：报告须写明已完成、当前停点、剩余工作、风险、下一步；完成声明须指向可核验证据。

## 验证命令

```bash
npm run build      # 必须零死链（config.ts 中 ignoreDeadLinks: false）
npm run dev        # 本地预览（默认 http://localhost:5173）
git status         # 确认 TEMP/、pem 等未入库
./deploy.sh        # 手动部署（构建→ossutil 上传 OSS→刷新 CDN）；push main 走 GitHub Actions 自动部署
```

## 完成标准

- **内容变更**：build 零死链 + 用户预览确认 + `docs/log.md` 有记录。
- **结构变更**（增删板块/改导航）：同步更新 `config.ts`、`docs/public/llms.txt`、相关 index，死链清零。
- 站点行为以构建产物为准，不以对话声明为准。
